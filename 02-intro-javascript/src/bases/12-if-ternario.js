const activo = true;
let mensaje = '';

/*if normal*/
// if (activo) {
//     mensaje = 'activo';
// } else {
//     mensaje = 'in activo';
//}


/*if ternario normal con salida del else*/
//mensaje = (activo) ? 'activo' : 'inactivo';

/*if corto sin salida del else*/
mensaje = (activo) && 'activo'



console.log(mensaje);